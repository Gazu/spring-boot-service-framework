package com.smbtech.serviceframework.httpclient.domain;

/** Defines supported connection reuse policy values. */
public enum ConnectionReusePolicy {
    /** Represents default. */
    DEFAULT,
    /** Represents always. */
    ALWAYS,
    /** Represents never. */
    NEVER,
    /** Represents other. */
    OTHER
}
