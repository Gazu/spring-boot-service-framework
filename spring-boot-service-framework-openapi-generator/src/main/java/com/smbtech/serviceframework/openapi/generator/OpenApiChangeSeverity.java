package com.smbtech.serviceframework.openapi.generator;

/** Defines supported open api change severity values. */
public enum OpenApiChangeSeverity {
    /** Represents breaking. */
    BREAKING,
    /** Represents non breaking. */
    NON_BREAKING
}
